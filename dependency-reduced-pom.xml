<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.appu</groupId>
  <artifactId>KafkaTemperatureAnalytics</artifactId>
  <version>0.1</version>
  <build>
    <plugins>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>jacoco-initialize</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>jacoco-site</id>
            <phase>verify</phase>
            <goals>
              <goal>report</goal>
            </goals>
            <configuration>
              <excludes>
                <exclude>**/com/appu/constants/**</exclude>
                <exclude>**/com/appu/equipment**</exclude>
              </excludes>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <output>file</output>
          <append>true</append>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.avro</groupId>
        <artifactId>avro-maven-plugin</artifactId>
        <version>${avro.version}</version>
        <executions>
          <execution>
            <phase>generate-sources</phase>
            <goals>
              <goal>schema</goal>
            </goals>
            <configuration>
              <sourceDirectory>${project.basedir}/src/main/resources/schemas/avro/equipment/</sourceDirectory>
              <includes>
                <include>equipment-key.avsc</include>
                <include>equipment-value-upgrade.avsc</include>
              </includes>
              <outputDirectory>${project.build.directory}/generated-sources</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>io.confluent</groupId>
        <artifactId>kafka-schema-registry-maven-plugin</artifactId>
        <version>${confluent.version}</version>
        <configuration>
          <schemaRegistryUrls>
            <param>${schemaRegistryUrl}</param>
          </schemaRegistryUrls>
          <userInfoConfig>${schemaRegistryBasicAuthUserInfo}</userInfoConfig>
          <subjects>
            <equipment-key>src/main/resources/schemas/avro/equipment/equipment-key.avsc</equipment-key>
            <equipment-value>src/main/resources/schemas/avro/equipment/equipment-value-upgrade.avsc</equipment-value>
          </subjects>
          <outputDirectory />
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven-compiler.version}</version>
        <configuration>
          <source>${maven.compiler.source}</source>
          <target>${maven.compiler.target}</target>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven-shader.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer />
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <repositories>
    <repository>
      <id>confluent</id>
      <url>https://packages.confluent.io/maven</url>
    </repository>
  </repositories>
  <dependencies>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>1.18.20</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.kafka</groupId>
      <artifactId>kafka-streams-test-utils</artifactId>
      <version>2.5.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <properties>
    <commons.version>2.10.1</commons.version>
    <maven-shader.version>3.2.4</maven-shader.version>
    <confluent.version>6.2.0</confluent.version>
    <zookeeper.version>3.7.2</zookeeper.version>
    <maven-compiler.version>3.5.1</maven-compiler.version>
    <gson.version>2.2.4</gson.version>
    <checkstyle.suppressions.location>checkstyle/supressions.xml</checkstyle.suppressions.location>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <avro.version>1.11.3</avro.version>
    <jcommander.version>1.48</jcommander.version>
    <junit.version>4.13.2</junit.version>
    <kafka-streams-test-utils.version>2.5.0</kafka-streams-test-utils.version>
    <schemaRegistryUrl>http://localhost:8081</schemaRegistryUrl>
    <java.version>1.8</java.version>
    <maven.compiler.source>11</maven.compiler.source>
    <slf4j.json>2.0.0-alpha1</slf4j.json>
    <jacoco.version>0.8.5</jacoco.version>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <kafka.version>2.5.0</kafka.version>
    <lombok.version>1.18.20</lombok.version>
    <maven.compiler.target>11</maven.compiler.target>
    <jackson-core.version>2.17.1</jackson-core.version>
    <kstream.version>2.5.0</kstream.version>
  </properties>
</project>
